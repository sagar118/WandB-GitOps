name: "Unit Test"
on:
    push:
        branches:
            - main
    workflow_dispatch:

jobs:
    unit-test:
        runs-on: ubuntu-latest
        steps:
            - name: Run unit-test on sortColors
              run: pytest Assignment-1/test_sort_colors.py
